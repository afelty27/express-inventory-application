extends layout 

block content 
    h1= title

    p To create a new Item object, fill out the form below: 

    form(method="POST" action='')
        div.form-group 
            label(for='name') Item Name: 
            input#name.form-control(type='text', placeholder='Item name here', name='name', required='true' value=(undefined===item ? "" : item.name))
        div.form-group
            label Category: 
            div     
                for category in category_list 
                    div(style='display: inline; padding-right:10px;')
                        input.checkbox-input(type='checkbox', name='category', id=category._id, value=category._id, checked=category.checked)
                        label(for=category._id) #{category.name}
        div.form-group 
            label(for='description') Description: 
            input#description.form-control(type='text', placeholder='Item Description Here', name='description', required='true' value=(undefined===item ? "" : item.description))
        .form-group 
            label(for="price") Price: 
            input#price.form-control(type='number', placeholder="Enter Price Here", name='price', required='true' min="0" value=(undefined===item ? 0 : item.name))
        .form-group 
            label(for="stock") Stock: 
            input#stock.form-control(type='number', placeholder="Enter Stock Quantity", name='stock', required='true' min='0' value=(undefined===item ? 0 : item.number_in_stock))

        button.btn.btn-primary(type='submit') Submit

