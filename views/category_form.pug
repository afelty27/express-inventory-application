extends layout

block content
  h1= title
  
  p To create a new category, fill out the form below: 

  form(method='POST' action='')
    div.form-group 
        label(for='name') Cateogry: 
        input#name.form-control(type='text', placeholder="Adventure Sports, Indoor Sports, etc.", name='name', required='true' value=(undefined===category ? '' : category.name))
    button.btn.btn-primary(type="submit") Submit 

    if errors 
        ul 
            for error in errors 
                li!= error.msg

